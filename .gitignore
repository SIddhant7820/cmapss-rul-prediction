# Python
__pycache__/
*.py[cod]
*.pyo
.env
*.egg-info/

# extra npy files (old duplicates, not the real processed data)
data/X_test.npy
data/X_train.npy
data/y_test.npy
data/y_train.npy

# Virtual Environment
rul_cmapss_env/

# Raw data (too large for github, and not needed for the deployed app)
data/raw/*.txt
data/raw/*.parquet

# Logs (but keep the plots the app needs)
logs/*.log
logs/*.csv

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.cursor
.cursorrules